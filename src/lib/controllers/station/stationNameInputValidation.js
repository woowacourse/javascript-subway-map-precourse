export default class StationNameInputValidation {
  constructor(inputValue) {
    this.inputValue = inputValue;
  }

  // 1. 지하철 역 이름은 2글자 이상이어야 한다. (이건 쉬우니까 이거부터!)

  // 2. 중복된 지하철 역 이름은 등록될 수 없다.

  // 3. 체크 결과 리턴하기.
}
