export default class Line {
  constructor() {
    this.registeredStations = [];
  }

  createLine() {
    const line = document.querySelector('#line-name-input').value;
    // setItem();
    // clearInput(lineNameInput);
  }
}
