import Component from '../../library/core/component.js';

class StationManager extends Component {
  constructor($target, props) {
    super($target, props);
    console.log('station- manager');
  }
}

export default StationManager;
