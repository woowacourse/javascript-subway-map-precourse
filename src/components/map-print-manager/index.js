import Component from '../../library/core/component.js';

class MapPrintManager extends Component {
  constructor($target, props) {
    super($target, props);
    console.log('map print - manager');
  }
}

export default MapPrintManager;
